package cn.ok.examples.ruleexecutionorder

import cn.ok.domains.Message;

rule "Rule2"
    when
        m : Message( status == Message.GOODBYE, message : message.substring(0, 10) )
    then
        System.out.println( "Rule2: " + message );
end
